#! /usr/bin/env bash

for arg in "$@"; do
  case "$arg" in
  -R)
    pacman -Qeq | fzf --preview 'pacman -Si {1}' | xargs -ro sudo pacman -R
    ;;
  -S)
    pacman -Slq | fzf --preview 'pacman -Si {1}' | xargs -ro sudo pacman -S
    ;;
  esac
done
